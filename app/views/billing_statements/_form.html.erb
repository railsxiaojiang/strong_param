<%= form_for(billing_statement) do |f| %>
  <% if billing_statement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(billing_statement.errors.count, "error") %> prohibited this billing_statement from being saved:</h2>

      <ul>
      <% billing_statement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
